import { auth } from "../../src/middleware/auth";

describe('auth middleware', () => {
  it('passes', () => {
    expect(1).toEqual(1)
  })
  //token is revoked

  //user is signing up or logging in

  //set req user code
})