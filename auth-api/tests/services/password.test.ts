import { hashPassword, validatePassword, confirmNewPassword } from "../../src/services/password";

describe('password service', () => {
  it('validate matching passwords', () => {
    expect(1).toBe(1);
  });

  
  it('validate non-matching passwords', () => {
    expect(1).toBe(1);
  });

  it('confirmation password matches', () => {
    expect(1).toBe(1);
  });

  it('confirmation password does not match', () => {
    expect(1).toBe(1);
  });

  it('hash a password', () => {
    expect(1).toBe(1);
  });


})